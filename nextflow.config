// nextflow.config - uses pre-activated conda environment
process {
  executor = 'local'
}

docker {
  enabled = true
}

profiles {
  docker {
    docker.enabled = true
    process {
      withName:CallONT {
        container = 'hkubal/clair3:latest'
      }
    }
  }
}