// nextflow.config - uses pre-activated conda environment
process {
  executor = 'local'
}

profiles {
  docker {
    docker.enabled = true
    process {
      withName:CallONT {
        container = 'hkubal/clair3:latest'
      }
    }
  }
}
